#include <stdio.h> 
#include <string.h> 

int wildcmp(char *s1, char *s2);
{
  char *foo;
  char *bar;
  
  if (strcmp(foo, bar) == 0)
    puts("Strings equal");
  else 
    puts("Strings do not equal");
  
  return 0;
}
