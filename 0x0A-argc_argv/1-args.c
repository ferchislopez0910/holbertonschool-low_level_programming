#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 * @argc: contador
 * @argv: matriz
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	if (argv[argc] != '\0')
	{}
	return (0);
}
