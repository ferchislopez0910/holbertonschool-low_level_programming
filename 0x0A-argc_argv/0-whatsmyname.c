#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 * @argc: contador
 * @argv: matriz
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	if (argc > 1)
	{}
	return (0);
}
